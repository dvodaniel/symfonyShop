<div class="container-fluid" style="background-color:#24214c;">
    <div class="row">
        <div class=container>
            <nav class="navbar navbar-expand-lg">
                <a class="navbar-brand" href="{{ url ('homepage') }}">
                    <img src="{{ asset ('images/ando-logo.jpeg') }}" alt="logo" width="80" height="80">
                </a>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Produse
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                {% for category in categories %}
                                   <div><a href="{{ url ('category', {'category':category.id}) }}">{{ category.name }}</a><div>
                                {% endfor %}
                            </div>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url ('contactInfo') }}">Informatii</a>
                        </li>s
                    </ul>
                    <ul class="navbar-nav mr-5">
                        <li class="nav-item d-flex justify-content-end">
                            <a class="nav-link" href="{{ url('user') }}">Contul meu</a>
                        </li>
                    </ul>
                    <form class="form-inline my-2 my-lg-0">
                        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                    </form>
                </div>
            </nav>
        </div>
    </div>
</div>
